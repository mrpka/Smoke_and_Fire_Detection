# Fire & Smoke Detection — dependencies
# Install with: pip install -r requirements.txt

opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0

# Optional: deep-learning layer (comment out if not needed)
# ultralytics>=8.0.0
